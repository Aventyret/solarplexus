{
  "name": "solarplexus",
  "version": "1.9.0",
  "private": true,
  "description": "",
  "author": "Äventyret",
  "license": "GPL-2.0-or-later",
  "keywords": [
    "WordPress",
    "block"
  ],
  "homepage": "https://aventyret.com",
  "repository": "",
  "main": "build/index.js",
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.1",
    "@wordpress/scripts": "^23.7.1",
    "prettier": "^2.7.1",
    "vitepress": "^0.22.3"
  },
  "scripts": {
    "build": "wp-scripts build",
    "format:check": "wp-scripts format --debug-check && prettier admin/*.php includes/**/*.php --debug-check",
    "format": "wp-scripts format && prettier admin/*.php includes/**/*.php --write",
    "lint:js": "wp-scripts lint-js",
    "lint:php": "./vendor/bin/phplint",
    "lint": "yarn lint:js && yarn lint:php",
    "packages-update": "wp-scripts packages-update",
    "start": "wp-scripts start",
    "test": "yarn format:check && yarn lint",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs"
  },
  "dependencies": {
    "@wordpress/date": "^4.15.0",
    "@wordpress/icons": "^9.6.0",
    "@wordpress/server-side-render": "^3.13.0"
  }
}
